import { type FpResolvedConfig } from "../config.js";
/**
 * Helper function to determine which headers to redact
 */
export declare function getRedactedQueryParams(resolvedConfig?: FpResolvedConfig): Set<string>;
