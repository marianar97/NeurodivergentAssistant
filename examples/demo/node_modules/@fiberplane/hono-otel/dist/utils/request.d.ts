import type { Attributes } from "@opentelemetry/api";
import type { GlobalResponse, HonoResponse, InitParam, InputParam } from "../types.js";
type PossibleHeaders = Headers | HonoResponse["headers"] | GlobalResponse["headers"];
export declare function headersToObject(headers: PossibleHeaders): Record<string, string>;
/**
 * Helper to get the request attributes for the root request.
 *
 * Requires that we have a cloned request, so we can get the body and headers
 * without consuming the original request.
 */
export declare function getRootRequestAttributes(request: Request, honoEnv: unknown): Promise<Attributes>;
export declare function getRequestAttributes(input: InputParam, init?: InitParam): Attributes;
export declare function getResponseAttributes(response: GlobalResponse | HonoResponse): Promise<Attributes>;
export {};
